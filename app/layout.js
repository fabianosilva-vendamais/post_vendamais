export const metadata = { title: 'VendaMais Content Engine', description: 'Newsletter + conteúdo social + geração visual' };
export default function RootLayout({ children }) { return <html lang="pt-BR"><body style={{ margin: 0 }}>{children}</body></html>; }
