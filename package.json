{
  "name": "vendamais-content-engine",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "node scripts/sync-app.mjs && next dev",
    "build": "node scripts/sync-app.mjs && next build",
    "start": "next start",
    "lint": "next lint",
    "supabase:push": "supabase db push",
    "supabase:functions": "supabase functions deploy text-generate image-generate source-fetch trends-fetch publish-schedule"
  },
  "dependencies": {
    "next": "^15.0.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}